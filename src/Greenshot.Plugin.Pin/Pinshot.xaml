<Window x:Class="Greenshot.Plugin.Pin.Pinshot"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        AllowsTransparency="True" 
        Background="Transparent" 
        WindowStyle ="None"
        MouseWheel="Window_MouseWheel">

    <Border x:Name="MainBorder" 
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Background="Black">
        <Border.Effect >
            <!--  
            <DropShadowEffect Direction="0" BlurRadius="10" ShadowDepth="0" Color="#1e80ff" /> 
            <DropShadowEffect Direction="0" BlurRadius="10" ShadowDepth="0" Color="#1e80ff" />
            
            -->
            <DropShadowEffect x:Name="shadowEffect" Direction="0" BlurRadius="10" ShadowDepth="0" />
        </Border.Effect>

        <!-- 添加右键菜单 -->
        <Border.ContextMenu>
            <ContextMenu>
                <MenuItem Header="关闭" Click="CloseMenuItem_Click" />
                <MenuItem Header="保存" Click="SaveMenuItem_Click" />
                <MenuItem Header="取消" Click="CancelMenuItem_Click" />
            </ContextMenu>
        </Border.ContextMenu>

        <!-- 图片控件，Source在代码中动态设置 -->
        <Image x:Name="MainImage" 
               Stretch="Uniform" 
               HorizontalAlignment="Left"
               VerticalAlignment="Top"/>
    </Border>
</Window>